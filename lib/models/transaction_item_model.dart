class TransactionItem {
  final String title, subtitle, trailing;
  final bool withderawel;
  const TransactionItem({
    required this.title,
    required this.subtitle,
    required this.withderawel,
    required this.trailing,
  });
}
